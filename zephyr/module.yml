name: zmk-keyboard-ploopy
build:
  depends:
    - zmk-driver-pmw3360
    - zmk-behavior-sensor-attr-cycle
    - zmk-driver-rp2040-sleep
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
